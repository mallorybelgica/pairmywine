import React from "react";
import styled from "styled-components";

const AccountInfo = () => {
  return <Wrapper>You have successfully logged out.</Wrapper>;
};

export default AccountInfo;

const Wrapper = styled.div`
  font-family: "Montserrat", sans-serif;
`;
